<%= render 'admin/data_fetch_tasks/task_info_card', task: task %>

<% if task.review_stage? %>
  <%= turbo_frame_tag "task_apply_form", src: edit_admin_author_list_parsing_path(task.author, task.id),
    target: "_top" %>
<% end %>
